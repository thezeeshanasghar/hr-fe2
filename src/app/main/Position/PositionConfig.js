import Position from './Position';

export const PositionConfig = {
    settings: {
        layout: {
            config: {}
        }
    },
    routes  : [
        {
            path     : '/position',
            component: Position
        }
    ]
};
